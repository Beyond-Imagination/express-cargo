## Пользовательский преобразователь

Вы можете преобразовывать строковые массивы, логические значения и т.д.

```typescript
class Request {
    @Query('tags')
    tags!: string[]; // Преобразует "tag1,tag2" в ['tag1', 'tag2']
}
```
