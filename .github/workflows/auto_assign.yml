name: Auto Assign

on:
    pull_request:
        types: [opened]

permissions:
    contents: read
    pull-requests: write

jobs:
    auto-assign:
        runs-on: ubuntu-latest

        steps:
            - uses: kentaro-m/auto-assign-action@v2.0.2
